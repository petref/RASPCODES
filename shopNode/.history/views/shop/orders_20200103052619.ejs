<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <main>
                <% if (orders.length <= 0 ) {%>
                    <h1>Nothing there!</h1>
                    <% } else {%>
                        <ul>
                            <% order.forEach(order => { %>
                                <li></li>
                                <% } %>
                        </ul>
                        <% } %>
            </main>
            <%- include('../includes/end.ejs') %>